# ===========================================
# ComfyUI-omni-llm 依赖配置文件
# ===========================================
# 安装说明：
# 1. 基础安装：pip install -r requirements.txt
# 2. 如果遇到版本冲突，请执行：
#    pip install qwen-tts --no-deps --user
# 3. 如果遇到 auto-gptq 版本解析问题，请手动安装：
#    pip install auto-gptq --no-deps --user
# 4. 如果遇到 webrtcvad 编译问题（缺少 Python.h），这是嵌入式 Python 环境限制，
#    可以跳过安装，不影响核心功能
# 5. Windows CUDA 12.8 需要取消对应 Python 版本的 llama-cpp-python 注释
# ===========================================

# ===========================================
# 基础依赖（必须安装）
# ===========================================
torch
torchaudio
numpy>=1.20.0
Pillow
requests
scipy>=1.7.0

# ===========================================
# 音频处理依赖
# ===========================================
soundfile
gguf
librosa>=0.10.0
pydub
resampy
ffmpeg-python
webrtcvad
audioread
soxr
numba
lazy_loader
msgpack
decorator

# ===========================================
# 图像处理增强
# ===========================================
opencv-python
scikit-image
albumentations

# ===========================================
# Qwen3-TTS 专用依赖（qwen_tts自带的site-packages已包含部分依赖）
# ===========================================
einops>=0.7.0
onnxruntime>=1.16.0
onnxruntime-gpu; sys_platform != 'darwin'
webdataset>=1.0.0
pydantic>=2.0.0
tqdm

# ===========================================
# Qwen3-ASR 专用依赖（qwen_asr自带的site-packages已包含部分依赖）
# ===========================================
nagisa==0.2.11
soynlp==0.0.493
qwen-omni-utils

# ===========================================
# 模型依赖
# ===========================================
modelscope>=1.20.0
huggingface-hub>=0.20.0
safetensors>=0.4.0
peft>=0.8.0
omegaconf>=2.3.0
tokenizers>=0.15.0
protobuf>=3.20.0
sacremoses

# ===========================================
# 量化优化依赖
# ===========================================
bitsandbytes
accelerate
sentencepiece
optimum

# ===========================================
# 多媒体处理
# ===========================================
torchvision
imageio
imageio-ffmpeg
av>=10.0.0
decord

# ===========================================
# 音频编解码补充
# ===========================================
pyaudio

# ===========================================
# 网络和数据处理
# ===========================================
urllib3>=1.26.0
certifi>=2022.0.0
charset-normalizer
idna

# ===========================================
# 配置文件处理
# ===========================================
pyyaml
toml
tomli
json5

# ===========================================
# 进程管理
# ===========================================
psutil
setproctitle

# ===========================================
# 音频处理补充
# ===========================================
kaldiio
pytorch-wpe

# ===========================================
# 机器学习基础
# ===========================================
scikit-learn>=1.1.0
pooch>=1.1.0
threadpoolctl>=3.2.0
joblib>=1.0.0
typing_extensions>=4.1.1

# ===========================================
# UMAP 降维（Fun-ASR 需要）
# ===========================================
umap-learn>=0.5.0
pynndescent>=0.5.0

# ===========================================
# 中文处理（Fun-ASR 需要）
# ===========================================
jieba>=0.42.0
jaconv
jamo

# ===========================================
# 阿里云相关（Fun-ASR 需要）
# ===========================================
oss2>=2.19.0
aliyun-python-sdk-core>=2.13.12
aliyun-python-sdk-kms>=2.4.1
crcmod>=1.7
pycryptodome>=3.4.7

# ===========================================
# TensorBoard 支持
# ===========================================
tensorboardX>=2.6.0

# ===========================================
# 编辑距离计算（Fun-ASR 需要）
# ===========================================
editdistance

# ===========================================
# llama-cpp-python (官方版本)


# ===========================================
# Fun-ASR 依赖
# ===========================================
# 方式 1: 直接安装（推荐）
funasr>=1.3.0

# ===========================================
# OpenAI 兼容调用依赖
# ===========================================
openai>=1.0.0
tiktoken

# ===========================================
# llms-py API 网关支持
# ===========================================
llms-py

# ===========================================
# 安装后验证
# ===========================================
# 安装完成后，运行以下命令验证：
# python -c "import llama_cpp; print('llama-cpp-python 版本:', llama_cpp.__version__)"
# python -c "import funasr; print('funasr 版本:', funasr.__version__)"
# python -c "import soundfile; print('soundfile 导入成功')"
# python -c "import librosa; print('librosa 版本:', librosa.__version__)"
# python -c "import transformers; print('transformers 版本:', transformers.__version__)"
