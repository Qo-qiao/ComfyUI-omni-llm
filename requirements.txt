# ===========================================
# ComfyUI-omni-llm 依赖配置文件
# ===========================================
# 安装说明：
# 1. 基础安装：pip install -r requirements.txt
# 2. 如果遇到 llama-cpp-python 问题，请手动安装适合您系统的版本
# 3. site-packages 目录中已包含部分依赖，无需重复安装
# ===========================================

# ===========================================
# 核心基础依赖（必须安装）
# ===========================================
torch
torchaudio
Pillow
requests
numpy
scipy>=1.7.0

# ===========================================
# 音频处理核心依赖
# ===========================================
soundfile

# ===========================================
# 可选依赖（功能增强，按需安装）
# ===========================================
# 如果需要使用 Supertonic TTS，安装：
# onnxruntime>=1.16.0
# onnxruntime-gpu; sys_platform != 'darwin'

# 如果需要使用 ASR 功能，安装：
# librosa>=0.10.0

# ===========================================
# 安装后验证
# ===========================================
# 安装完成后，运行以下命令验证：
# python -c "import llama_cpp; print('llama-cpp-python 版本:', llama_cpp.__version__)"
# python -c "import numpy; print('numpy 导入成功')"
# python -c "import torch; print('torch 导入成功')"
